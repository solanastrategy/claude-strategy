# web dashboard

run the web dashboard locally.

1. `pnpm --filter apps/web install`
2. `pnpm --filter apps/web dev`

the dashboard is read-only and shows treasury composition, nav, and decision logs.
